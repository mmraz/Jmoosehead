package org.mraz.moose.dao.enums.db.moose;
import java.util.Map;
import java.util.HashMap;
import com.felees.hbnpojogen.persistence.impl.StringValuedEnum;
/**
 * Defines the PlayerGenderEnum enumeration.
 * 
 * @author autogenerated
 *
 */
public enum PlayerGenderEnum  implements StringValuedEnum {

	/** Enum Constant. */
	MALE("male"),
	/** Enum Constant. */
	FEMAILE("femaile"),
	/** Enum Constant. */
	NONE("none");
	/**
	 * Internal storage of status field value, see the Enum spec for
 	 * clarification.
 	 */
	private final String status;
	
	/**
	 * Enum constructor for ActiveState.
	 * @param state Value.
	 */
	PlayerGenderEnum(final String state) {
		this.status = state;
	}
	
	/** Construct a map for reverse lookup. */
	private static Map<String, PlayerGenderEnum> valueMap = new HashMap<String, PlayerGenderEnum>();

    static {
    	// construct hashmap for later possible use.
        for (PlayerGenderEnum unit : values()) {
            valueMap.put(unit.getValue(), unit);
        }
    }
    
	/**
	 * Current string value stored in the enum.
	 * 
	 * @return string value.
	 */
	public String getValue() {
		return this.status;
	}

	/**
     * Perform a reverse lookup (given a value, obtain the enum).
     * 
     * @param value to search
     * @return Enum object.
     */
    public static PlayerGenderEnum lookupEnum(String value) {
        return PlayerGenderEnum.valueMap.get(value);
    }
}
